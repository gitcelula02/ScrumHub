services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: scrumhub-backend
    ports:
      - "${HOST_BACKEND_PORT:-3000}:3000"
    env_file:
      - backend/.env
    environment:
      - NODE_ENV=production
      - PORT=3000
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    networks:
      - scrumhub-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: scrumhub-frontend
    ports:
      - "${HOST_FRONTEND_PORT:-8080}:8080"
    environment:
      - NODE_ENV=production
      - VITE_API_URL=${VITE_API_URL_DOCKER:-http://localhost:3000/api}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - scrumhub-network

networks:
  scrumhub-network:
    driver: bridge