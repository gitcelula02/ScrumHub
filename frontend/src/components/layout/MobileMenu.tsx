import React from 'react';

/**
 * @component MobileMenu
 * Responsive mobile menu for small screens.
 */
export const MobileMenu: React.FC = () => {
  return (
    <div className="md:hidden fixed bottom-0 left-0 right-0 h-16 bg-sidebar-bg border-t border-border flex items-center justify-around px-4">
      {/* Mobile nav items */}
    </div>
  );
};
